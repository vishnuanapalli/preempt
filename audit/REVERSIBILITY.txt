Migration reversibility — 2026-07-29
====================================

The harness is proven able to fail. It is NOT evidence the migrations are reversible:
the baseline creates no schema objects, so `covered` is 0 and there is nothing to reverse.

    docker compose up -d --wait
    cd api && uv run pytest tests/test_reversibility.py
    -> 7 passed, 1 xfailed

The xfail is the honest result. It is `strict=True, raises=AssertionError` — strict so the
day S-010 adds a table it fails and the marker must be deleted; `raises` because otherwise
pytest turns a fixture exception into XFAIL and a harness that computed nothing emits the
same line.

Failability: `test_an_irreversible_migration_is_reported` injects a migration that creates a
table and drops nothing, and fails if the round trip calls it reversible.

Where no database is reachable (CI before 41b1e85, any fresh clone) these tests skip with a
reason naming what is unproven. `-ra` is in pytest's addopts so the skip prints; under `-q` a
skip is a dot and a run that proved nothing looks like one that proved everything. CI now
provisions the database, so they run: run 30493278207, `42 passed, 1 xfailed`, zero skips.

TRIMMED 2026-07-29. This file was 193 lines and the harness 1,429; they are now 25 and 599.
The removed machinery — a 22-case mutation matrix over the harness, chain stepping for a
chain of one, seven speculative object classes — verified the harness rather than the
application, against a migration that does nothing. Ledger R9/R10, Preempt D-016. It is all
in git at `22d656b`; restore what S-010 needs when S-010 exists.

Known gaps, not a bound: row data, grants, ownership, triggers, policies, types, routines,
view and sequence internals, TimescaleDB objects, branched histories. A downgrade that
destroys a table's contents while restoring its shape reads as reversible.
